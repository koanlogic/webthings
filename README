CoRE protocols implementation and ramblings.

    docs/
        misc documents (IETF drafts, presentations, ...)

    bits/
        code (evcoap, kink, ...)

    sys/
        kernel and base system (kink, wsn, ...)
